//------------------------------------------------------------------------------
//<auto-generated>
//    This code was generated from a template.
//    Manual changes to this file will be overwritten if the code is regenerated.
//</auto-generated>
//------------------------------------------------------------------------------

$(document).ready(function () {
    getListDocumentRepository();
});

function getListDocumentRepository(page) {
    page = page != undefined ? page : 1;
    var pageSize = $('#DocumentRepository-page_select').val();
    var element = {
        table: '#DocumentRepository-table',
        tbody: '#DocumentRepository-tbody',
        from: '#DocumentRepository-from_page',
        to: '#DocumentRepository-to_page',
        total: '#DocumentRepository-total',
        pagination: '#DocumentRepository-pagination',
        item_pagination: 'DocumentRepository-item'
    }
    var param = {
        sort: {
            field: "id",
            type: 0
        },
        start: page,
        length: pageSize
    }
    RequestData('POST', "/v1/DocumentRepository/list", element.table, element.tbody, JSON.stringify(param), function (data) {
        if (data.succeeded) {
            $(element.tbody).html('');
            $(element.pagination).html('');
            if (data.list.length > 0) {
                SetTableData(true, 12, element, {
                    page: page,
                    pageSize: pageSize,
                    count: data.count,
                    function_name: 'getListDocumentRepository'
                }, function (count) {
                    data.list.forEach(function (item) {
                        $(element.tbody).append(`
                            <tr>
                                <td class="text-nowrap text-center">${count}</td>
								<td class="text-nowrap">${item.base64}</td>
								<td class="text-nowrap">${item.code}</td>
								<td class="text-nowrap">${item.description}</td>
								<td class="text-nowrap">${item.filename}</td>
								<td class="text-nowrap">${item.modul}</td>
								<td class="text-nowrap">${item.subject}</td>

                                <td class="text-center">
                                    <div>
                                        <button type="button" data-toggle="dropdown" class="btn btn-sm btn-info dropdown-toggle" aria-hashpopup="true" aria-expanded="false">Aksi</button>
                                        <div class="dropdown-menu">
                                            <a href="#!" class="dropdown-item" onclick="detailDocumentRepositoryDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Detail</a>
                                            <a href="#!" class="dropdown-item" onclick="editDocumentRepositoryDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Edit</a>
                                            <a href="#!" class="dropdown-item" onclick="deleteDocumentRepositoryDialog(${item.id})">Delete</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `);
                        count++;
                    });
                });
            } else {
                settabledata(false, 12, element);
            }
        } else {
            ShowNotif(`${data.message} : ${data.description}`, "error");
        }
    })
}

