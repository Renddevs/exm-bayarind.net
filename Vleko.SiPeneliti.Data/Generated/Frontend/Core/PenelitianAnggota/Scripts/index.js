//------------------------------------------------------------------------------
//<auto-generated>
//    This code was generated from a template.
//    Manual changes to this file will be overwritten if the code is regenerated.
//</auto-generated>
//------------------------------------------------------------------------------

$(document).ready(function () {
    getListPenelitianAnggota();
});

function getListPenelitianAnggota(page) {
    page = page != undefined ? page : 1;
    var pageSize = $('#PenelitianAnggota-page_select').val();
    var element = {
        table: '#PenelitianAnggota-table',
        tbody: '#PenelitianAnggota-tbody',
        from: '#PenelitianAnggota-from_page',
        to: '#PenelitianAnggota-to_page',
        total: '#PenelitianAnggota-total',
        pagination: '#PenelitianAnggota-pagination',
        item_pagination: 'PenelitianAnggota-item'
    }
    var param = {
        sort: {
            field: "id",
            type: 0
        },
        start: page,
        length: pageSize
    }
    RequestData('POST', "/v1/PenelitianAnggota/list", element.table, element.tbody, JSON.stringify(param), function (data) {
        if (data.succeeded) {
            $(element.tbody).html('');
            $(element.pagination).html('');
            if (data.list.length > 0) {
                SetTableData(true, 12, element, {
                    page: page,
                    pageSize: pageSize,
                    count: data.count,
                    function_name: 'getListPenelitianAnggota'
                }, function (count) {
                    data.list.forEach(function (item) {
                        $(element.tbody).append(`
                            <tr>
                                <td class="text-nowrap text-center">${count}</td>
								<td class="text-nowrap">${item.birthDate}</td>
								<td class="text-nowrap">${item.birthPlace}</td>
								<td class="text-nowrap">${item.fullname}</td>
								<td class="text-nowrap">${item.idPenelitian}</td>
								<td class="text-nowrap">${item.nik}</td>
								<td class="text-nowrap">${item.nip}</td>
								<td class="text-nowrap">${item.profession}</td>
								<td class="text-nowrap">${item.sort}</td>

                                <td class="text-center">
                                    <div>
                                        <button type="button" data-toggle="dropdown" class="btn btn-sm btn-info dropdown-toggle" aria-hashpopup="true" aria-expanded="false">Aksi</button>
                                        <div class="dropdown-menu">
                                            <a href="#!" class="dropdown-item" onclick="detailPenelitianAnggotaDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Detail</a>
                                            <a href="#!" class="dropdown-item" onclick="editPenelitianAnggotaDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Edit</a>
                                            <a href="#!" class="dropdown-item" onclick="deletePenelitianAnggotaDialog(${item.id})">Delete</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `);
                        count++;
                    });
                });
            } else {
                settabledata(false, 12, element);
            }
        } else {
            ShowNotif(`${data.message} : ${data.description}`, "error");
        }
    })
}

