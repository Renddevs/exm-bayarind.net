//------------------------------------------------------------------------------
//<auto-generated>
//    This code was generated from a template.
//    Manual changes to this file will be overwritten if the code is regenerated.
//</auto-generated>
//------------------------------------------------------------------------------

$(document).ready(function () {
    getListPenelitianWorkflow();
});

function getListPenelitianWorkflow(page) {
    page = page != undefined ? page : 1;
    var pageSize = $('#PenelitianWorkflow-page_select').val();
    var element = {
        table: '#PenelitianWorkflow-table',
        tbody: '#PenelitianWorkflow-tbody',
        from: '#PenelitianWorkflow-from_page',
        to: '#PenelitianWorkflow-to_page',
        total: '#PenelitianWorkflow-total',
        pagination: '#PenelitianWorkflow-pagination',
        item_pagination: 'PenelitianWorkflow-item'
    }
    var param = {
        sort: {
            field: "id",
            type: 0
        },
        start: page,
        length: pageSize
    }
    RequestData('POST', "/v1/PenelitianWorkflow/list", element.table, element.tbody, JSON.stringify(param), function (data) {
        if (data.succeeded) {
            $(element.tbody).html('');
            $(element.pagination).html('');
            if (data.list.length > 0) {
                SetTableData(true, 12, element, {
                    page: page,
                    pageSize: pageSize,
                    count: data.count,
                    function_name: 'getListPenelitianWorkflow'
                }, function (count) {
                    data.list.forEach(function (item) {
                        $(element.tbody).append(`
                            <tr>
                                <td class="text-nowrap text-center">${count}</td>
								<td class="text-nowrap">${item.callbackUrl}</td>
								<td class="text-nowrap">${item.groupNo}</td>
								<td class="text-nowrap">${item.idPenelitian}</td>
								<td class="text-nowrap">${item.idUserRequester}</td>
								<td class="text-nowrap">${item.navigationUrl}</td>
								<td class="text-nowrap">${item.statusCode}</td>
								<td class="text-nowrap">${item.statusDescription}</td>
								<td class="text-nowrap">${item.stepNo}</td>
								<td class="text-nowrap">${item.workflowCode}</td>
								<td class="text-nowrap">${item.workflowName}</td>

                                <td class="text-center">
                                    <div>
                                        <button type="button" data-toggle="dropdown" class="btn btn-sm btn-info dropdown-toggle" aria-hashpopup="true" aria-expanded="false">Aksi</button>
                                        <div class="dropdown-menu">
                                            <a href="#!" class="dropdown-item" onclick="detailPenelitianWorkflowDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Detail</a>
                                            <a href="#!" class="dropdown-item" onclick="editPenelitianWorkflowDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Edit</a>
                                            <a href="#!" class="dropdown-item" onclick="deletePenelitianWorkflowDialog(${item.id})">Delete</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `);
                        count++;
                    });
                });
            } else {
                settabledata(false, 12, element);
            }
        } else {
            ShowNotif(`${data.message} : ${data.description}`, "error");
        }
    })
}

